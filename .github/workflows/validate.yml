name: Validar proyecto

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
    paths:
      - "web/**"
      - "android/**"
      - "patches/**"
      - "supabase/**"
      - "scripts/**"
      - ".github/workflows/**"
      - ".gitleaks.toml"

permissions:
  contents: read

jobs:
  validate:
    name: Validar Combusplus
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v6

      - name: Comprobar secretos expuestos
        shell: bash
        run: |
          set -euo pipefail
          curl -sSfL \
            https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz \
            | tar -xz gitleaks

          config_args=()
          if [[ -f "$GITHUB_WORKSPACE/.gitleaks.toml" ]]; then
            config_args+=(--config "$GITHUB_WORKSPACE/.gitleaks.toml")
          else
            echo "::warning::.gitleaks.toml no existe; se utilizará la configuración predeterminada de Gitleaks."
          fi

          ./gitleaks dir "$GITHUB_WORKSPACE" \
            --no-banner \
            --redact \
            "${config_args[@]}"

      - name: Configurar Node 24
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Probar frontend generado
        working-directory: web
        run: |
          set -euo pipefail
          npm ci
          npm run check

      - name: Configurar Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: "v2.8.1"

      - name: Comprobar todas las funciones Supabase
        run: deno check supabase/functions/**/*.ts

      - name: Probar la lógica del backend
        run: deno test supabase/tests

      - name: Validar funciones nuevas
        run: |
          python - <<'PY'
          from pathlib import Path

          app = Path("web/src/app.js").read_text(encoding="utf-8")
          index = Path("web/index.html").read_text(encoding="utf-8")

          checks = {
              "función de depósito única":
                  app.count(
                      "async function runFullTankSearch({"
                  ) == 1,
              "destino disponible":
                  'id="quickDestination"' in index,
              "backend de ruta":
                  "route-recommend" in app,
              "desvío máximo":
                  "maxDetourKm" in app,
              "resumen de ruta":
                  "renderRouteSummaryV106" in app,
          }

          failed = [
              name
              for name, ok in checks.items()
              if not ok
          ]

          if failed:
              raise RuntimeError(
                  "Validaciones fallidas: "
                  + ", ".join(failed)
              )

          print(
              "Combusplus 10.6.3 validado correctamente"
          )
          PY
